"""Conformance rule modules.

Every module in this package is imported automatically by the registry, so a
new rule becomes active as soon as the file exists. Group rules by the area of
the document they inspect rather than by checkpoint number: a contributor
working on tables should not have to edit the same file as one working on
fonts.
"""
